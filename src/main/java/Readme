技术细节
1.在浏览器提交一个请求
2.跳转到一个jsp页面，该jsp页面加载了一个js文件
3.改js文件出发一个ajax请求，请求person表所有的数据
4.客户端接收到数据，动态地写入到 table中
5.具有CRUD功能
-----------------------------------------------------------------------------------------------------------------------
细节问题：
	1.在wen项目中hibernate的配置文件中必须把
		方言和驱动加上
	2.在strut2的配置文件中,继承的是json-default,结果集result的类型type=json
	3.在action中有一个属性，并且属性有get方法，属性的值就会传递到客户端